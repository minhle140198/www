import GBlock from 'components/GBlock';
import { GrapesPluginType, IStorageManager, IStyleManager } from 'types/grapes';
import GComponent from './GComponent';
interface IProps {
    id: string;
    webpage: boolean;
    newsletter: boolean;
    plugins: GrapesPluginType[];
    components: GComponent[];
    blocks: GBlock[];
    storageManager: IStorageManager;
    blockManager: {};
    styleManager: IStyleManager;
}
declare function GEditor(props: IProps): JSX.Element;
declare namespace GEditor {
    var defaultProps: {
        blockManager: {};
        blocks: any[];
        components: any[];
        newsletter: boolean;
        plugins: any[];
        storageManager: {};
        styleManager: {};
        webpage: boolean;
    };
}
export default GEditor;
